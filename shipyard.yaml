apiVersion: "spec.keptn.sh/0.2.2"
kind: "Shipyard"
metadata:
  name: "shipyard"
spec:
  stages:
    - name: "dev"
      sequences:
        - name: "securitycheck"
          tasks:
            - name: "securityscan"
            - name: "outputmetrics"
